{
  "name": "doru-mall",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r css js dist/ && sed \"s|__SUPABASE_URL__|${SUPABASE_URL}|g; s|__SUPABASE_ANON_KEY__|${SUPABASE_ANON_KEY}|g\" index.html > dist/index.html"
  }
}
